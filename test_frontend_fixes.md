# 前端修复测试指南

## 修复内容总结

### 1. 主页面统计数据实时更新
**问题**: 意识协合度、记忆片段、互动次数不会实时更新
**修复**: 
- 在模块切换时自动刷新统计数据
- 添加了事件监听机制，子组件可以通知主组件刷新
- 添加了手动刷新方法

### 2. 知识库文档上传功能
**问题**: 文档上传没有调用后端接口，刷新后数据丢失
**修复**:
- 替换模拟上传为真实API调用
- 支持uni-app和标准web环境的文件上传
- 上传成功后自动刷新文档列表和统计数据

### 3. 数据同步机制
**问题**: 前端和后端数据不同步
**修复**:
- 文档上传后立即刷新本地数据
- 通过事件总线通知父组件更新统计
- 确保所有操作后数据保持一致

## 测试步骤

### 测试1: 主页面统计数据
1. 登录系统，查看左侧统计数据（意识协合度、记忆片段、互动次数）
2. 切换到不同模块，观察统计数据是否更新
3. 在记忆模块添加记忆片段，返回主页查看记忆片段数量是否增加

### 测试2: 文档上传功能
1. 进入知识库模块
2. 点击"上传文档"按钮
3. 选择文件并上传
4. 观察上传进度和成功提示
5. 检查文档是否出现在列表中
6. 刷新页面，确认文档仍然存在

### 测试3: 数据同步
1. 上传文档后，查看右上角"文档总数"是否更新
2. 返回主页，查看左侧统计数据是否反映新的文档数量
3. 在不同模块间切换，确认数据保持一致

## API调用验证

### 检查统计API
```bash
curl -X GET "http://localhost:8000/api/user/dashboard-stats?user_id=test_user_123"
```

### 检查文档列表API
```bash
curl -X GET "http://localhost:8000/api/user/documents?user_id=test_user_123"
```

### 检查文档上传API
```bash
curl -X POST "http://localhost:8000/api/user/documents/upload?user_id=test_user_123" \
  -F "file=@test_document.txt"
```

## 预期结果

1. **统计数据实时更新**: 左侧统计数据应该反映用户的真实数据，而不是固定的0
2. **文档上传成功**: 文档能够成功上传到后端，并在前端列表中显示
3. **数据持久化**: 刷新页面后，上传的文档仍然存在
4. **数据同步**: 前端显示的数据与后端API返回的数据一致

## 可能的问题和解决方案

### 问题1: 统计数据仍然显示0
**原因**: 后端数据库中没有对应用户的数据
**解决**: 确保用户已登录，并且数据库中有对应的记录

### 问题2: 文件上传失败
**原因**: 
- 网络连接问题
- 后端服务未启动
- 文件格式不支持
**解决**: 
- 检查后端服务状态
- 确认文件格式符合要求
- 查看浏览器控制台错误信息

### 问题3: 数据不同步
**原因**: 事件监听机制未正确工作
**解决**: 检查浏览器控制台是否有JavaScript错误

## 调试技巧

1. **查看网络请求**: 打开浏览器开发者工具的Network标签，观察API调用
2. **查看控制台日志**: 检查Console标签中的错误和日志信息
3. **检查后端日志**: 查看后端服务的日志输出
4. **数据库验证**: 直接查询数据库确认数据是否正确存储
